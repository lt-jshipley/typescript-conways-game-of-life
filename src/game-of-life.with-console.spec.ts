// import { gameOfLife } from './game-of-life.with-console'

describe('Game of Life With Console', () => {

  it('should do all the things we want it to do', () => {
    expect(true).toEqual(true);
  });

});